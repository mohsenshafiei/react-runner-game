const CANVAS_SIZE = [800, 600];

const SCALE = 40;
const SPEED = 100;

const DIRECTIONS = {
  37: [-1, 0], // left
  39: [1, 0] // right
};

const RUNNER_START = [0, 13]

export {
  CANVAS_SIZE,
  SCALE,
  SPEED,
  DIRECTIONS,
  RUNNER_START
};
